import streamlit as st import google.generativeai as genai # 1. Setup Gemini # Use Streamlit secrets to hide your key later genai.configure(api_key=st.secrets["GEMINI_API_KEY"]) model = genai.GenerativeModel("gemini-1.5-flash") st.title("Viral-Scan-PRO") # 2. Payment Section (Simple Paywall) st.sidebar.header("Upgrade to PRO") # Replace this URL with your actual Stripe or Lemon Squeezy payment link payment_url = "https://buy.stripe.com/your_link_here" st.sidebar.markdown(f"[Click here to pay and unlock PRO]({payment_url})") # 3. App Logic user_input = st.text_input("Enter your prompt for the AI:") if user_input: with st.spinner("Thinking..."): response = model.generate_content(user_input) st.write(response.text